<h1>Posts</h1>

<%= link_to("Add New Post", new_post_path) %><br/>

<% @posts.each do |post| %>
<tr>
  <td><%= link_to post.entry, post %></td>
  <td><%= post.score %></td>
  <td><%= button_to '+1', like_post_path(post), method: :put %></td>
 </tr>
<% end %>